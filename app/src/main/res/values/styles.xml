<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
        <item name="android:fontFamily" tools:ignore="NewApi">@font/open_sans_regular</item>
    </style>

    <style name="OrdinaryTextStyle" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@color/blueBlack</item>
        <item name="android:textColorSecondary">@color/blueBlack</item>
        <item name="android:fontFamily" tools:ignore="NewApi">@font/open_sans_regular</item>
    </style>

    <style name="Widget.BottomNavigationView"
        parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/open_sans_regular</item>
        <item name="android:textColor">@color/st_patrick_blue</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textSize">8sp</item>
    </style>
</resources>
