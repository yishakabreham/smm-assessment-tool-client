<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".activities.windowResult"
            android:exported="false" />
        <activity
            android:name=".activities.windowFinal"
            android:exported="false" />
        <activity
            android:name=".activities.phases.windowTesting"
            android:exported="false"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".activities.phases.windowDeployment"
            android:exported="false"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".activities.phases.windowImplementation"
            android:exported="false"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".activities.windowProjectList"
            android:exported="false" />
        <activity
            android:name=".activities.phases.windowDesign"
            android:exported="false"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".activities.windowLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.phases.windowRequirements"
            android:exported="false"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".activities.windowCreateProject"
            android:exported="false"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".activities.windowDashboard"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>